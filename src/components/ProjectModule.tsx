import React from 'react';
import styles from '../../styles/ProjectModule.module.scss';

const ProjectModule = () => {
  return (
    <li className={styles.modContainer}>
      <div className={styles.modWrapper}></div>
    </li>
  )
}

export default ProjectModule